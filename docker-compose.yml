services:
  watershed-operations:
    build: .
    image: ghcr.io/your-org/watershed-operations:latest
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
